apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hermes-frontend.fullname" . }}

data:
  config.js: |
    window.APP_CONFIG = {
      backendUrl: "{{ .Values.hermes.backendUrl }}",
    };
